#############################################
# Deprecated: Use the unified .env instead   #
#############################################
#
# The project now uses a single consolidated .env file at the repo root
# for all configuration (app + tests + CI guidance).
#
# Steps:
#   1) Open .env at the repo root
#   2) Paste your NVIDIA_API_KEY and APIFY_TOKEN
#   3) Optionally set TEST_REAL_PDF_PATH to enable the integration test
#   4) Run: pytest -q -k "not integration" (unit) or
#           pytest -q -m integration tests/test_nemo_extraction_integration.py
#
# You can remove this file after migrating.
