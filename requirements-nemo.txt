# NVIDIA NeMo Retriever Integration Requirements
# Install after base requirements: pip install -r requirements.txt -r requirements-nemo.txt
# Note: langchain-nvidia-ai-endpoints and faiss-cpu are already included in requirements.txt

# Core NeMo Integration (additional packages)
nvidia-riva-client>=2.14.0  # For NeMo NIM services (if self-hosting)

# GPU-Accelerated Vector Search (recommended for performance)
cupy-cuda12x>=12.0.0  # GPU acceleration for vector operations (CUDA 12.x)
cuvs-cu12>=24.04.0  # NVIDIA cuVS for GPU-accelerated vector search
cudf-cu12>=24.04.0  # GPU DataFrame operations

# Scientific computing for embeddings (CPU fallback)
scipy>=1.9.0  # Scientific computing for embeddings

# Enhanced Document Processing
unstructured>=0.10.0  # Advanced document parsing (complements NeMo Extraction)
transformers>=4.35.0  # For local model inference if needed
sentence-transformers>=2.2.0  # For embedding model fallbacks

# Performance and Monitoring
prometheus-client>=0.17.0  # Metrics collection for NeMo services
psutil>=5.9.0  # System resource monitoring
memory-profiler>=0.61.0  # Memory usage tracking

# Optional: Self-Hosted NIM Dependencies
# tritonclient[all]>=2.40.0  # For Triton Inference Server integration
# docker>=6.1.0  # For container management of self-hosted NIMs

# Development and Testing
pytest-asyncio>=0.21.0  # For async testing of NeMo services
httpx>=0.24.0  # Modern HTTP client for API testing
aiofiles>=23.1.0  # Async file operations

# Note: GPU packages require NVIDIA drivers and CUDA toolkit
# For CPU-only deployment, install: pip install -r requirements.txt
# For GPU deployment, install: pip install -r requirements.txt -r requirements-nemo.txt